# Core Domain Semantic Model

Place PBIP semantic model artifacts for the core domain in this folder.

Expected pattern:
- One shared semantic model per subject area.
- Model name format: sm_<domain>_<subject>.
- Environment bindings are applied during deployment, not committed per environment.
